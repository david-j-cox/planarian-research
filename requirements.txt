# Python >= 3.11 recommended (developed on 3.11.3).
# All HTTP calls use stdlib urllib.request — no `requests` dependency.

numpy>=2.0
pandas>=2.2
scipy>=1.13
opencv-python>=4.10
av>=14.0
matplotlib>=3.9
seaborn>=0.13
tqdm>=4.66
scikit-image>=0.24   # midline/skeleton extraction (open_dish_tracker --extract_midline)
scikit-learn>=1.5

